# Cloudflare Stability Scripts

Executable checks and probes for Cloudflare runtime verification.

Typical uses:
- smoke tests
- runtime audits
- release evidence capture

Preferred root aliases:
- `npm run runtime:audit`
- `npm run smoke:api`
- `npm run smoke:api:full`
- `npm run release:log`
