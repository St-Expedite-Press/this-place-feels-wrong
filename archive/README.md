# Archive

Archived, non-live repository material.

- `anglossic_quiz/`
  - legacy quiz product material

Nothing under this directory is part of the live build or deployment path.

The former `site-legacy/` static output snapshot was removed during cleanup. Use git history if that snapshot is needed.
