# Site Source Assets

Source CSS and JS copied into the generated `site/assets/` tree during build.

Use this directory for interior-site asset changes rather than editing the generated output tree directly.
