# Workflows

GitHub Actions workflows for this project.

Current workflows:

- `deploy-pages.yml` validates app output and tooling integrity, then publishes the Cloudflare Pages artifact.
- `api-health-monitor.yml` probes production API behavior on a schedule.
