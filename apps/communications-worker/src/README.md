# Communications Source

Source code for the Cloudflare communications worker.

Primary file:
- `index.ts`, which contains route handling, validation, CORS, rate limiting, upstream integrations, and response shaping
