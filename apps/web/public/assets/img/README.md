# Image Assets

Canonical image files mirrored exactly into `apps/web/public/assets/img/`. Store every shipped PNG, JPEG, WebP, and SVG variant here; sync does not generate formats.

Edit the source copy, then run `npm run assets:sync` and `npm run assets:check`.
