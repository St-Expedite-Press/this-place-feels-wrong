# Site Source

Editable source for the generated public website.

Structure:
- `layouts/` for page shells
- `components/` for shared Astro components
- `pages/` for route entrypoints, including the homepage
- `data/` for shared metadata
- `public/assets/` for authored CSS, JS, and media
