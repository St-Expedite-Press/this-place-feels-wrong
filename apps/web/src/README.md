# Site Source

Editable source for the generated public website.

Structure:
- `layouts/` for page shells
- `partials/` for shared fragments
- `pages/` for generated page templates, including the homepage
- `assets/` for source CSS, JS, and media
- `data/` for shared metadata
- `static/` for files copied verbatim into `dist/site/`
