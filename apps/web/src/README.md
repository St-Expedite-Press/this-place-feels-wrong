# Site Source

Editable Astro source for the generated public website.

Structure:

- `layouts/` for page shells
- `components/` for shared Astro components
- `pages/` for route entrypoints, including the homepage
- `data/` for shared metadata used by templates

Authored CSS, JS, and media live in `apps/web/public/assets/`, not under `src/`.
