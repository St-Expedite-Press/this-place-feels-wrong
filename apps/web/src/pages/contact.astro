---
import Base from '../layouts/Base.astro';
import siteData from '../data/site.json';

const page = siteData.pages.contact;
const styles = [
  'assets/css/tokens.css',
  'assets/css/interior-base.css',
  'assets/css/layout.css',
  'assets/css/components.css',
  'assets/css/forms.css',
];
---
<Base
  title={page.title}
  description={page.description}
  canonical={page.canonical}
  subtitle={page.subtitle}
  eyebrow={page.eyebrow}
  introKicker={page.introKicker}
  introTitle={page.introTitle}
  introText={page.introText}
  currentNav={page.currentNav}
  styles={styles}
>
  <section class="form-trust form-trust--two">
    <div class="trust-card trust-card--center">
      <strong>General inquiry</strong>
      Rights, press, collaborations, editorial questions, and general correspondence.
    </div>
    <div class="trust-card trust-card--center">
      <strong>Submission inquiry</strong>
      Fiction, novella, reprints, translation proposals. Project type, length, stage, and ask.
    </div>
  </section>

  <section class="split-card-grid split-card-grid--balanced">
    <article class="form-card form-shell form-card--tall">
      <div>
        <p class="card-kicker">General</p>
        <h2 class="section-heading">General inquiry</h2>
        <p class="section-copy section-copy--large">Short note, direct reply.</p>
      </div>
      <form class="form-grid" id="contact-form">
        <input id="contact-website" name="website" type="text" tabindex="-1" autocomplete="off" aria-hidden="true" hidden />
        <div class="field">
          <label for="contact-reason">Reason</label>
          <select class="select" id="contact-reason" name="reason">
            <option value="General">General</option>
            <option value="Rights">Rights</option>
            <option value="Press">Press</option>
            <option value="Collaboration">Collaboration</option>
          </select>
        </div>
        <div class="field">
          <label for="contact-email">Email</label>
          <input class="input" id="contact-email" name="email" type="email" autocomplete="email" placeholder="your@email.com" required />
        </div>
        <div class="field">
          <label for="contact-message">Message</label>
          <textarea class="textarea" id="contact-message" name="message" placeholder="What should we know before replying?" required></textarea>
        </div>
        <div class="actions">
          <button class="button" id="contact-submit" type="submit">Send message</button>
        </div>
      </form>
      <div class="status-panel" id="contact-helper" role="status" aria-live="polite" hidden>
        <div data-status-text></div>
        <div class="status-panel__actions">
          <a class="status-action" id="contact-mailto-fallback" href="mailto:editor@stexpedite.press">Open email</a>
          <button class="status-action" id="contact-copy" type="button">Copy fallback</button>
        </div>
      </div>
    </article>

    <article class="form-card form-shell form-card--tall">
      <div>
        <p class="card-kicker">Submissions</p>
        <h2 class="section-heading">Submission inquiry</h2>
        <p class="section-copy section-copy--large">Keep it concise. Project type, length, stage, and ask.</p>
      </div>
      <form class="form-grid" id="submit-form">
        <input id="submit-website" name="website" type="text" tabindex="-1" autocomplete="off" aria-hidden="true" hidden />
        <div class="field">
          <label for="submit-email">Email</label>
          <input class="input" id="submit-email" name="email" type="email" autocomplete="email" placeholder="your@email.com" required />
        </div>
        <div class="field">
          <label for="submit-note">Project note</label>
          <textarea class="textarea" id="submit-note" name="note" placeholder="Project type, length, stage, and what you are asking for."></textarea>
        </div>
        <div class="actions">
          <button class="button" id="submit-button" type="submit">Send inquiry</button>
        </div>
      </form>
      <div class="status-panel" id="submit-helper" role="status" aria-live="polite" hidden>
        <div data-status-text></div>
        <div class="status-panel__actions">
          <a class="status-action" id="submit-mailto-fallback" href="mailto:editor@stexpedite.press">Open email</a>
          <button class="status-action" id="submit-copy" type="button">Copy fallback</button>
        </div>
      </div>
    </article>
  </section>

  <Fragment slot="scripts">
    <script is:inline src="/assets/js/site-shell.js" type="module"></script>
    <script is:inline src="/assets/js/contact-page.js" type="module"></script>
  </Fragment>
</Base>
