# Static Source Files

Files copied verbatim into the generated `dist/site/` output.

Use this directory for static support files such as `CNAME`, `robots.txt`, and archived support pages that are still published verbatim.
