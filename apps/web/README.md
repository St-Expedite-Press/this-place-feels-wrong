# Web App

Public site source for St. Expedite Press.

- authored source: `apps/web/src/`
- generated output: `dist/site/`
- build entrypoint: `scripts/build-site.mjs`

The homepage and all interior pages are now generated from source. No checked-in public output directory under the repo root is used for deployment.
