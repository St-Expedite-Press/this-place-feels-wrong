# Docs

Project documentation root.

Subdirectories:
- `infrastructure/` for Worker, D1, and email setup
- `operations/` for runbooks and release records
- `ontology/` for machine-readable and human-readable repo structure summaries
